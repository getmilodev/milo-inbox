name: Fix canonical URLs
on: workflow_dispatch
permissions:
  contents: write
jobs:
  fix-urls:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Replace www URLs with non-www in remaining files
        run: |
          sed -i 's|https://www.getmilo.dev|https://getmilo.dev|g' index.html blog.html executives.html
          git diff --stat
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --staged --quiet || (git commit -m "fix: update remaining canonical and Schema.org URLs to non-www" && git push)
